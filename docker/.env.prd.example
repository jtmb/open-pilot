# Copy this file to docker/.env.prd and fill in the values.
# This file is intentionally NOT committed to source control.

# Cloudflare Tunnel token.
# Found at: https://one.dash.cloudflare.com → Networks → Tunnels → <your tunnel> → Configure → Token
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
